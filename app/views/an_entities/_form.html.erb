<%= form_for(@an_entity) do |f| %>
  <% if @an_entity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@an_entity.errors.count, "error") %> prohibited this an_entity from being saved:</h2>

      <ul>
      <% @an_entity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attr1 %><br />
    <%= f.text_field :attr1 %>
  </div>
  <div class="field">
    <%= f.label :attr2 %><br />
    <%= f.text_field :attr2 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
